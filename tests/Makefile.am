# Do not install.
check_SCRIPTS = release.test watchdog.test
CLEANFILES = $(check_SCRIPTS)

# Added scripts to list of tests to run.
TESTS = $(check_SCRIPTS)

# Add additional things to distribution
EXTRA_DIST = release watchdog
